{
  "name": "reliable-notifier",
  "version": "0.0.2",
  "description": "A replacement for bad email notifiers",
  "main": "index.js",
  "scripts": {
    "start": "npm install && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.3",
    "puppeteer": "^5.4.1",
    "twilio": "^3.51.0"
  }
}
